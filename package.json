{
  "name": "digital-wallet",
  "version": "1.0.0",
  "description": "digital-wallet pasta raiz",
  "main": "index.js",
  "scripts": {
    "compose:up": "docker-compose up -d --build",
    "compose:down": "docker-compose down --remove-orphans",
    "logs": "docker-compose logs -f"
  }
}